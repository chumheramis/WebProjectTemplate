// I'm not familiar with grunt for now, but just in case your a grunt master.
module.exports = function (grunt) {
    'use strict';
    grunt.initConfig({

    })
    grunt.registerTask('compile', []);
};
